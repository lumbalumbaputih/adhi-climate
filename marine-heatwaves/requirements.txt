# Marine-heatwave analysis: minimal, reproducible stack.
# Statistics (OLS, Pearson, Mann-Kendall, Sen's slope, Pettitt change-point)
# are implemented in stats_utils.py and validated in test_stats.py, and the
# Hobday detection is implemented in build_dataset.py and validated in
# test_project.py, so neither scipy nor a NetCDF stack is required.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
