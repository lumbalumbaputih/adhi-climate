# Fremantle sea-level analysis: minimal, reproducible stack.
# Statistics (OLS, Pearson, Mann-Kendall, Sen's slope, Pettitt change-point)
# are implemented in stats_utils.py and validated in test_stats.py; the
# quadratic acceleration fit lives in analysis.py and is validated in
# test_project.py. scipy is NOT required.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
