# Raw source data: too large for GitHub (IBTrACS 74MB, ERSSTv5 147MB) and
# freely re-downloadable from the sources documented in each README.
# Reproduce by placing the originals in cyclone-risk/data/raw/.
**/data/raw/
*.nc

# Python virtual environment (local only; recreate with requirements.txt)
.venv/
venv/

# Python caches
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS noise
.DS_Store

# Node build tooling for the portfolio (local only)
node_modules/
package-lock.json
